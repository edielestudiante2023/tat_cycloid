<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is NOT for an existing file/dir in project root,
    # forward it to the public/ folder
    RewriteCond %{REQUEST_URI} !^/enterprisesstph/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
